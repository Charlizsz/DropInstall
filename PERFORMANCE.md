# 0.5 速度验证

2026-09-23，在本机 Apple Silicon Mac 上，比较 0.4 安装核心与 0.5 安装核心。两者均以 `swiftc -O` 构建，使用相同 DMG、相同临时目标目录和同名目标文件夹；未修改已安装的用户应用。

测试包包含约 136 MiB 应用数据（128 MiB 主数据及 2,000 个 4 KiB 小文件）。每个版本运行三次；表中比较第 2、3 次的中位数，排除第一次挂载与后续缓存状态不同的影响。

| 项目 | 0.4 | 0.5 |
| --- | ---: | ---: |
| 重名检查到弹出完成，重复打开 | 1.901 秒 | 0.235 秒 |
| 确认前复制整个应用 | 是 | 否 |
| 本次重名预检的 `hdiutil info` 调用 | 4 次 | 3 次 |
| 等待命令结束 | 50 ms 轮询 | 完成事件通知 |

该场景耗时约减少 88%。0.4 的两次已缓存挂载分别为 0.227、0.228 秒；复制应用分别为 0.881、0.874 秒。0.5 的两次已缓存挂载分别为 0.114、0.106 秒，重名时不再执行复制。完整命令计时包含进程启动和等待，不能解释为纯磁盘读写耗时。

0.4 首次测试的挂载耗时为 4.094 秒，整项 5.820 秒。没有把它与 0.5 的缓存后数据比较，也没有据此声称所有安装或首次挂载都能加速 88%。默认映像完整性检查没有关闭。确认覆盖时需要重新挂载并复制；相同 DMG 的多个获准覆盖项目合并处理，已经安装过的非重名项目不会再次复制。

本次原始计时保存在 `build/performance-before.log` 和 `build/performance-after.log`。它们是本地验证产物，不打包进 App。

实机界面验证中，位于 iCloud 文稿目录的自建测试 DMG 有一次在系统磁盘映像挂载服务里等待超过两分钟，停止该测试子进程后清理。相同内容复制到本地临时目录后正常进入重名确认窗口。没有把它归因于某个已证实的 iCloud 故障，也没有宣称该类系统挂载等待已解决。Finder 服务短名称和本地文件的完整预检流程均已在界面验证。
